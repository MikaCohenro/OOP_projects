/**
 * interface for a renderer type
 */
interface Renderer {
    /**
     * A method that gets a board and shows it
     *
     * @param board board class obj
     */
    void renderBoard(Board board);
}
